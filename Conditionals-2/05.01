using System;
public class Program {
    public static string Puzzle(string s) {
 		if(s.Length<=3) return "short";
		 if(s.Length>=4 && s.Length<=7) return "average";
		 if(s.Length>=8 && s.Length<=14) return "long";
		 
        else return "super long";
    }
}
